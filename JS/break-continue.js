
//similar to c++
for (var i = 1; i <= 10; ++i) {
    if (i <= 5) continue;
    else {
        console.log(`Find the number: ${i}`);
        break;
    }
}

//Dry run: check the code before run. Main Reason is to find the errors(logical, compilation, runtime error etc).